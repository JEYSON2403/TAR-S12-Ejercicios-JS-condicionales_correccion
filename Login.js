//Login

//ejercicio echo en clases


console.log("LOGIN");
let username;
username= JYanza;//correcto
let key;
key=12345;
let Username;
Username= JYANZA;
let Key;
Key = 2468;
let denegado 
if(username==Username && key==Key){
    console.log("Succesful Session star");
    
}
else{
    console.log("incorrec password or user")
}


/*Este código es un ejemplo que simula un proceso de autenticación de usuario utilizando variables 
para almacenar el nombre de usuario y contraseña. En este caso, se utiliza un condicional "if" 
para comparar si los valores de las variables "username" y "key" 
coinciden con los valores de las variables "Username" y "Key". Si ambos valores coinciden,
 se muestra un mensaje indicando que la sesión se inició con éxito. De lo contrario, 
 se muestra un mensaje indicando que la contraseña o el usuario son incorrectos.

La especificación rápida de este código podría ser:

Nombre del programa: Autenticación de usuario
Objetivo: Verificar si el usuario y contraseña ingresados son correctos para iniciar sesión
Variables utilizadas: "username", "key", "Username" y "Key"
Mensajes de salida: "Successful Session start" (sesión iniciada con éxito) o "incorrect password or user" 
(contraseña o usuario incorrectos)*/